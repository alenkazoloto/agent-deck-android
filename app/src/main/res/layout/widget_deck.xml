<?xml version="1.0" encoding="utf-8"?>
<!--
  Two lines: what is waiting, and the one it would open. A widget that showed a list would be
  a list nobody can scroll on a 2x1 tile; a widget that showed only a number would answer
  "how many" and not "which".
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:padding="14dp"
    android:background="?android:attr/colorBackground">

    <TextView
        android:id="@+id/widget_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textStyle="bold"
        android:textSize="15sp"
        android:textColor="?android:attr/textColorPrimary"
        android:text="@string/app_name" />

    <TextView
        android:id="@+id/widget_top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:textSize="13sp"
        android:textColor="?android:attr/textColorSecondary"
        android:text="@string/app_name" />
</LinearLayout>
