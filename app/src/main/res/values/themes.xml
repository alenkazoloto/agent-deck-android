<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns every pixel; this exists only so the window has no action bar and
         the right background before the first frame. -->
    <style name="Theme.AgentDeck" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
